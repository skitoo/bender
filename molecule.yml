---
molecule:
  test:
    # sequence of commands to run when performing `molecule test`
    sequence:
      - syntax
      - create
      - converge
      - idempotence
      - verify

ansible:
  playbook: playbook.yml

docker:
  containers:
  - name: bender
    image: debian
    image_version: latest
