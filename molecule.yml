---
ansible:
  playbook: playbook.yml

docker:
  containers:
    - name: bender
      image: debian
      image_version: latest
      image: ssiegel/base-debian-systemd
      privileged: True
      command: /sbin/init
