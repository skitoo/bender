---
molecule:
  test:
    # sequence of commands to run when performing `molecule test`
    sequence:
      - syntax
      - create
      - converge
      - idempotence
      - verify

ansible:
  playbook: playbook.yml

docker:
  containers:
  - name: bender-01
    ansible_groups:
      - group1
    image: debian
    image_version: latest
  - name: bender-02
    ansible_groups:
      - group2
    image: debian
    image_version: latest
